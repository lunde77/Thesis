##Procedure before push:


### to pull

git status # se stauts 

git pull origin main # hvis ikke opdatet, så hent opdatering fra master



### to push

git add .  # add nye ting

git commit -m "SKRIV DATO "

git push -u origin main # push changes, derved er dine ændringer fuldført





---------------------------------


failure distribution 
- if they chech every day
- if they check every month 


increase samples 
